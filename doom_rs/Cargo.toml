[package]
name = "doom_rs"
version = "0.1.0"
edition = "2024"

# This crate's src/ is fully generated by `transpiler --emit-rust` and
# regenerated wholesale on every run - never hand-edit files under src/.
# See transpiler/src/codegen/ and /home/michael/.claude/plans/
# eager-marinating-axolotl.md for the generation pipeline and design.

[dependencies]
